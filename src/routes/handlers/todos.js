'use strict';

const Handlers = {

    fetchTasks: (request, h) => {
    },

    createTask: (request, h) => {
    },

    updateTask: (request, h) => {
    },

    deleteTask: (request, h) => {
    }

};

export default Handlers;
